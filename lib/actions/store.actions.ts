"use server";
import prisma from "../prisma/prisma";

// same as /api/store route.ts
// export async function CreateStore(name: string) {
//   const store = await prisma.store.create({
//     data: {
//       name,
//       userId: "testid",
//     },
//   });
// }
